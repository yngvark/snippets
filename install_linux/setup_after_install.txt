** Install chrome **

** Install rclone **

rclone config:
* add remote dropbox: Bare følg defaultsettings

`$ rclone copy dropbox:/linux linux --verbose`

Take it from there.

FOLDER STRUCTURE:

~/yk $ tree -d -L 3
.
├── app
├── git
├── rclone
│   ├── dropbox
│   │   └── linux
│   └── enc
│       └── path
└── tmp

20 directories
