** Install chrome **

** Install Dropbox ** (fra nettsiden)
Sett lokasjon til: ~/yk/Dropbox

** Install rclone **

rclone config:
* add remote dropbox, navn dropbox: Bare følg defaultsettings
* add remote crypt
  * name> enc
  * Storage> crypt
  * remote> dropbox:/rclone
  * filename_encryption>1
    * 1=standard
  * dirctory_name_encryption> 1
    * 1=true
  * Password or pass phrase for encryption> y
    * y=Yes type in my own password
  * password:
    * Enter secret from 1password>rclone
  * Password or pass phrase for salt > y
    * y=Yes type in my own password
    * Enter secret from 1password>rclone
  * Edit advanced config? > n
  
mkdir -p ~/yk/app
mkdir -p ~/yk/git
mkdir -p ~/yk/rclone/enc
mkdir -p ~/yk/tmp
mkdir -p ~/tmp

cd ~/yk/rclone/dropbox
rclone copy dropbox:/linux linux --verbose

Gjør ett og ett steg i
* _install_all.sh
* _utvikling.sh

FOLDER STRUCTURE:

~/yk $ tree -d -L 3
.
├── app
├── git
├── rclone
│   ├── Dropbox
│   │   └── linux
│   └── enc
│       └── path
└── tmp

20 directories
